<section class="cart py-16">
  <div class="order container mx-auto w-1/2">
    <% if(session.cart) { %>
    <div class="flex items-center border-b border-gray-300 pb-4">
      <img src="/image/cart-black.png" alt="" />
      <h1 class="font-bold ml-4 text-2xl">order summary</h1>
    </div>
    <div class="food-list">
      <% for(let pizza of Object.values(session.cart.items)) { %>
      <div class="flex items-center py-8">
        <img class="w-24" src="image/<%= pizza.item.image %>" alt="" />
        <div class="flex-1 ml-4">
          <h1><%= pizza.item.name %></h1>
          <span><%= pizza.item.size %></span>
        </div>
        <span class="flex-1"> <%= pizza.qty %> pcs </span>
        <span class="font-bold text-lg">$<%= pizza.item.price * pizza.qty %></span>
      </div>
      <% } %>

      <!-- <div class="flex items-center py-8">
        <img class="w-24" src="image/pizza.png" alt="" />
        <div class="flex-1 ml-4">
          <h1>motPizza</h1>
          <span>medium</span>
        </div>
        <span class="flex-1"> 1 pcs </span>
        <span class="font-bold text-lg">$300</span>
      </div>
      <div class="flex items-center py-8">
        <img class="w-24" src="image/pizza.png" alt="" />
        <div class="flex-1 ml-4">
          <h1>motPizza</h1>
          <span>medium</span>
        </div>
        <span class="flex-1"> 1 pcs </span>
        <span class="font-bold text-lg">$300</span>
      </div>
      <div class="flex items-center py-8"> -->
        <!-- <img class="w-24" src="image/pizza.png" alt="" />
        <div class="flex-1 ml-4">
          <h1>motPizza</h1>
          <span>medium</span>
        </div> -->
        <!-- <span class="flex-1"> 1 pcs </span>
        <span class="font-bold text-lg">$300</span> -->
      </div>
    <hr>
    <div class="text-right py-4">
        <div>
            <span class="text-lg font-bold">Total Amount:</span>
            <span class="amount text-2xl font-bold ml-2">$<%= session.cart.totalPrice %></span>
        </div>
        <% if(user) { %>
        <div>
            <form action="/orders" method="POST" class="mt-12">
                <input class="border border-gray-400 p-2 w-1/2 outline-none mb-4" type="text" placeholder="Phone Number" name="phone">
                <input class="border border-gray-400 p-2 w-1/2 outline-none" type="text" placeholder="Address" name="address">
                <div>
                    <button type="submit" class="btn-primary px-6 py-2 rounded-full text-white font-bold mt-6 bg-orange-600">order Now</button>
                </div>
            </form>
        </div>
        <% } else { %>
        <a href="/login" class="inline-block cursor-pointer btn-primary px-6 py-2 rounded-full text-white font-bold mt-6 capitalize">login to continue</a>
        <% } %>
    </div>
    <% } else { %>

  <div class="empty-cart py-16">
        <div class="container mx-auto text-center">
            <h1 class="text-3xl font-bold mb-2">cart empty </h1>
            <p class="text-gray-500 text-lg mb-12">you probably haven't ordered a pizza yet. <br> to order a pizza , go to the main page.</p>
            <img class="w-2/5 mx-auto" src="/image/empty-cart.png" alt="empty-cart">
            <a href="/" class="inline-block px-6 py-2 rounded-full btn-primary text-white font-bold mt-12">Go back</a>
        </div>
    </div>
    <% }  %>

  </div>
</section>
